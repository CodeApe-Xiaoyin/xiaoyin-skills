interface:
  display_name: "Neat"
  short_description: "Preserve context and converge project knowledge"
  default_prompt: "Use $neat checkpoint to plan a bounded context save, or $neat reconcile to plan a safe knowledge-base cleanup. Never write before I approve the plan."
